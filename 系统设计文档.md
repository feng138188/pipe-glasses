# AI管道施工眼镜 - 系统设计文档（纯摄像头方案）

## 1. 项目概述

### 1.1 项目背景
管道施工中，工人需要实时监测水管的坡度和高度，确保安装符合设计规范。传统方法依赖测量仪器，效率低且需要额外人员配合。本系统旨在通过AI眼镜实现实时、免手持的坡度与高度检测。

### 1.2 核心功能
- **实时坡度检测**：检测当前安装水管的倾斜角度
- **实时高度测量**：测量水管相对于地面的高度
- **AR可视化**：在视野中叠加显示测量数据
- **语音提示**：通过语音反馈安装状态

### 1.3 核心设计原则
> **只用眼镜，不需要任何外部设备或额外传感器**

仅依赖：
- 眼镜自带的摄像头（视觉检测）
- 眼镜内置的IMU（补偿头部倾斜，所有智能眼镜标配）

---

## 2. 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                     AI管道施工眼镜系统                        │
│                    （纯摄像头方案）                           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────────┐       ┌──────────────────────┐        │
│  │   摄像头（RGB）   │       │  内置IMU（标配）      │        │
│  │   水管图像采集    │       │  补偿头部倾斜角度     │        │
│  └────────┬─────────┘       └──────────┬───────────┘        │
│           │                            │                    │
│           ▼                            ▼                    │
│  ┌─────────────────────────────────────────────────────────┐│
│  │              图像 + 姿态 融合处理                        ││
│  └─────────────────────────────────────────────────────────┘│
│           │                                                 │
│           ▼                                                 │
│  ┌─────────────────────────────────────────────────────────┐│
│  │              水管识别与定位                              ││
│  │  • 边缘检测 + 霍夫直线                                  ││
│  │  • 或 YOLOv8-nano 轻量检测                              ││
│  └─────────────────────────────────────────────────────────┘│
│           │                                                 │
│     ┌─────┴─────┐                                           │
│     ▼           ▼                                           │
│  ┌────────┐  ┌────────────────┐                             │
│  │坡度计算│  │高度估计        │                             │
│  │角度提取│  │管径作为比例尺  │                             │
│  └───┬────┘  └───────┬────────┘                             │
│      │               │                                      │
│      ▼               ▼                                      │
│  ┌─────────────────────────────────────────────────────────┐│
│  │              AR显示 + 语音播报                           ││
│  │  • 坡度角度/百分比                                      ││
│  │  • 高度数值                                             ││
│  │  • 合格/不合格状态                                      ││
│  └─────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────┘
```

---

## 3. 坡度检测方案

### 3.1 原理

```
实际水管:
    ════════════════╗  ← 水管有坡度
                    ║
────────────────────── ← 水平面（重力方向垂直）

检测步骤:
1. 摄像头拍到水管图像
2. 提取水管边缘线
3. IMU提供当前头部倾斜角 θ_head
4. 水管在画面中的角度 θ_image
5. 真实坡度 = θ_image - θ_head
```

### 3.2 为什么需要IMU补偿

```
情况A：工人平视（头部水平）
┌──────────────────┐
│     ═══════╗     │  ← 画面中水管倾斜3°
│            ║     │     真实坡度 = 3° - 0° = 3° ✓
└──────────────────┘

情况B：工人低头5°
┌──────────────────┐
│   ══════════╗    │  ← 画面中水管倾斜8°
│             ║    │     真实坡度 = 8° - 5° = 3° ✓
└──────────────────┘

不补偿的话，情况B会误判为8°坡度！
```

### 3.3 算法流程

```
输入: 摄像头帧 + IMU姿态数据

Step 1: 图像预处理
  - 高斯模糊降噪
  - 自适应直方图均衡化（应对光照变化）

Step 2: 水管检测
  方案A（传统CV）:
    - Canny边缘检测
    - 霍夫直线变换
    - 过滤出近似水平的长线段
    - 合并为主线段
  
  方案B（深度学习）:
    - YOLOv8-nano检测水管区域
    - 在检测框内提取边缘线

Step 3: 角度计算
  - θ_image = arctan(Δy / Δx)  // 水管在画面中的角度
  - θ_head = IMU.pitch          // 头部俯仰角
  - θ_real = θ_image - θ_head   // 真实坡度

Step 4: 平滑滤波
  - 卡尔曼滤波或滑动平均
  - 消除抖动，输出稳定值

输出: 坡度角度（°）、坡度百分比（%）
```

### 3.4 精度分析

| 误差来源 | 量级 | 对策 |
|----------|------|------|
| 图像边缘提取 | ±0.5° | 多帧平均 |
| IMU漂移 | ±0.3° | 定期校准 |
| 镜头畸变 | ±0.2° | 相机标定 |
| **综合精度** | **±1°** | 满足施工需求 |

> 注：排水管道坡度通常要求精度±0.5%~1%，对应角度约±0.3°~0.6°。如需更高精度，可增加多帧融合。

---

## 4. 高度测量方案

### 4.1 方案：利用管道直径作为比例尺

```
原理：
已知水管直径 D（工人提前输入，如DN100 = 10cm）

摄像头看到的水管宽度 W_pixel（像素）
通过透视关系：

  实际高度 H = f(管径像素宽度, 相机焦距, 管道位置)

具体计算：
  距离 = (实际直径 × 焦距) / 像素宽度
  高度 = 距离 × sin(俯仰角) + 眼镜佩戴高度
```

### 4.2 计算示意

```
工人视角：
         眼镜位置（已知高度 H_eye ≈ 1.6m）
            │
            │  俯仰角 θ（IMU提供）
            │╲
            │ ╲  距离 d
            │  ╲
            │   ╲
────────────┼────●──── 水管位置
            │   
═══════════════════════ 地面

水管高度 = H_eye - d × sin(θ)

其中 d = (管径实际值 × 焦距) / 管径像素宽度
```

### 4.3 操作流程

1. 工人语音输入："管径100"（DN100）
2. 系统自动识别水管轮廓
3. 测量水管在画面中的像素宽度
4. 结合IMU俯仰角计算高度
5. AR显示高度值

### 4.4 精度分析

| 距离 | 精度估计 | 说明 |
|------|----------|------|
| 1m内 | ±3cm | 管径像素大，测量准 |
| 1-3m | ±5cm | 可接受范围 |
| 3m以上 | ±10cm | 建议靠近测量 |

---

## 5. 硬件选型

### 5.1 推荐方案

由于只需要摄像头+IMU，硬件选择范围更广：

| 设备 | 价格 | 摄像头 | IMU | 适合度 |
|------|------|--------|-----|--------|
| **RealWear Navigator 500** | ~$2000 | 48MP | ✓ | ⭐⭐⭐⭐⭐ 工业首选 |
| **Vuzix M400** | ~$1500 | 12MP | ✓ | ⭐⭐⭐⭐ 轻量化 |
| **Rokid Air Pro** | ~$500 | 有 | ✓ | ⭐⭐⭐ 性价比高 |
| **普通安全帽+手机** | ~$300 | 手机 | ✓ | ⭐⭐ 最低成本验证 |

### 5.2 最低成本验证方案

```
方案：安全帽 + 手机支架 + 手机

成本：~300元（手机支架）
优势：立即可以开始开发和测试
劣势：不够轻便，非AR显示

适合：原型验证阶段
```

### 5.3 量产方案

```
方案：定制轻量AR眼镜

核心组件：
- ARM芯片（如高通骁龙XR2）
- RGB摄像头（1200万像素以上）
- 6轴IMU（加速度计+陀螺仪）
- 微型显示屏（光波导或棱镜）
- 电池（3000mAh以上）

预估成本：$500-1500/台（量产）
```

---

## 6. 软件模块设计

### 6.1 技术栈

```
平台: Android (RealWear/Vuzix) 或 自定义Linux
语言: Python(原型) → C++/Java(量产)
视觉库: OpenCV + (可选)TensorFlow Lite
显示: OpenGL ES / AR SDK
```

### 6.2 模块划分

```
ai_pipe_glasses/
├── src/
│   ├── pipe_detector.py       # 水管检测（边缘+直线）
│   ├── slope_calculator.py    # 坡度计算（含IMU补偿）
│   ├── height_estimator.py    # 高度估计（管径比例尺）
│   ├── imu_handler.py         # IMU数据读取与滤波
│   ├── ar_display.py          # AR叠加显示
│   ├── voice_feedback.py      # 语音播报
│   └── main.py                # 主程序入口
├── config/
│   ├── camera_calib.yaml      # 相机标定参数
│   └── pipe_specs.yaml        # 管道规格参数
└── models/
    └── yolov8_pipe.tflite     # (可选)水管检测模型
```

---

## 7. AR显示设计

```
┌────────────────────────────────────────────┐
│                                            │
│                                            │
│         ══════════════╗                    │
│         ↗ 2.8°       ║  ← 坡度标注线      │
│                       ║                    │
│                                            │
│                                            │
│  ┌──────────────────────┐                  │
│  │ 坡度: 2.8° (4.9%)   │                  │
│  │ 高度: 0.85m          │                  │
│  │ 管径: DN100          │                  │
│  │ 状态: ✓ 合格         │                  │
│  └──────────────────────┘                  │
│                                            │
│  目标: 坡度 2°~5° | 高度 0.8~1.0m         │
└────────────────────────────────────────────┘

颜色规则：
  绿色 = 合格
  黄色 = 接近边界
  红色 = 超出范围
```

---

## 8. 使用流程

```
1. 开机 → 自动启动检测程序
         ↓
2. 语音设置 → "管径100，坡度3到5，高度0.8到1"
         ↓
3. 工人正常施工 → 眼镜实时检测
         ↓
4. AR显示 → 实时显示坡度和高度
         ↓
5. 语音提示 → "坡度合格" / "高度偏低，请调整"
         ↓
6. 数据记录 → 自动保存施工数据（可选）
```

---

## 9. 性能指标

| 指标 | 目标值 | 说明 |
|------|--------|------|
| 坡度精度 | ±1° | 满足大部分管道施工需求 |
| 高度精度 | ±5cm（3m内） | 基于管径比例尺 |
| 检测帧率 | ≥15fps | 实时反馈 |
| 检测延迟 | <200ms | 无明显滞后 |
| 续航 | ≥8小时 | 覆盖一个工作日 |
| 启动时间 | <5秒 | 开机即用 |

---

## 10. 开发计划

### 第一阶段：PC端原型验证（2周）
- [x] 实现水管边缘检测
- [x] 实现坡度角度计算
- [ ] 实现管径比例尺高度估计
- [ ] 用手机模拟IMU数据
- [ ] 录制测试视频验证

### 第二阶段：手机端验证（2周）
- [ ] 移植到Android手机
- [ ] 集成手机IMU
- [ ] 实现实时视频处理
- [ ] 现场测试精度

### 第三阶段：眼镜端集成（3周）
- [ ] 部署到AR眼镜
- [ ] 开发AR显示界面
- [ ] 实现语音交互
- [ ] 性能优化

### 第四阶段：现场测试（3周）
- [ ] 实验室精度标定
- [ ] 模拟工地测试
- [ ] 真实施工现场试用
- [ ] 收集反馈迭代

---

## 11. 风险与对策

| 风险 | 对策 |
|------|------|
| 光照太暗/太亮 | 自适应曝光 + 直方图均衡化 |
| 水管颜色与背景相近 | 形状检测为主，颜色为辅 |
| 工人头部晃动大 | 卡尔曼滤波平滑 + 稳定后才显示 |
| 管道被遮挡 | 提示"请调整视角" |
| 精度不够 | 多帧融合 + 靠近测量 |

---

## 12. 总结

**本方案的核心优势：**

1. **硬件极简** — 只需一个带摄像头的智能眼镜，无需额外传感器
2. **成本可控** — 最低用手机就能验证，量产成本可控
3. **操作简单** — 戴上就用，语音控制，完全免手持
4. **实时反馈** — AR叠加显示，施工过程中即时获得数据
